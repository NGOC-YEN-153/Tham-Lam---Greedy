#include <bits/stdc++.h>
using namespace std;
#define ll long long
ll mod=1e9+7;
int main () {
    ll n,k;cin>>n;ll a[n],b[n];for(ll i=0;i<n;i++)cin>>a[i];
    for(ll i=0;i<n;i++)cin>>b[i];
    sort(b,b+n);sort(a,a+n);
    ll ans=0;
    for(ll i=0;i<n;i++)ans+=a[i]*b[i];
    cout<<ans;
}
